# Fedora / RHEL

所有软件包共用软件源配置。已添加软件源时，请直接跳到第 2 步选择软件。安装命令使用 sudo。

## 1. 添加软件源

<!--@include: @/.vitepress/snippets/repositories/zh-CN/fedora-1.md-->

## 2. 选择软件

::: code-group

```sh [dae]
sudo dnf install dae
```

```sh [daed]
sudo dnf install daed
```

```sh [v2rayA]
sudo dnf install v2raya
```

```sh [v2ray]
sudo dnf install v2ray
```

```sh [Xray]
sudo dnf install xray
```

```sh [Juicity]
sudo dnf install juicity
```

```sh [Juicity-rs]
sudo dnf install juicity-rs
```

```sh [v2ray-rules-dat]
sudo dnf install v2ray-rules-dat
```

:::
